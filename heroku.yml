
  RUN: git clone https://github.com/kappithannemo/plugin-resp.git \
       && cd plugin-resp \

  && virtualenv -p /usr/bin/python3 venv \
 && . ./venv/bin/activate \
 
  && pip install -r requirements.txt \

  && python3 main.py


